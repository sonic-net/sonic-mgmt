spine0:
  sonic:
    config: |
      sudo counterpoll tunnel enable 
      sudo config interface startup D1D3P1 
      sudo config interface startup D1D3P2
      sudo config interface startup D1D4P1
      sudo config interface startup D1D4P2
      sudo config interface ipv6 enable use-link-local-only D1D3P1 
      sudo config interface ipv6 enable use-link-local-only D1D3P2
      sudo config interface ipv6 enable use-link-local-only D1D4P1 
      sudo config interface ipv6 enable use-link-local-only D1D4P2
    deconfig: |
      sudo config interface ipv6 disable use-link-local-only D1D3P1 
      sudo config interface ipv6 disable use-link-local-only D1D3P2
      sudo config interface ipv6 disable use-link-local-only D1D4P1 
      sudo config interface ipv6 disable use-link-local-only D1D4P2
      sudo counterpoll tunnel disable

  bgp:
    config: |
      no router bgp
      router bgp 65202
      no bgp ebgp-requires-policy
      no bgp default ipv4-unicast
      bgp disable-ebgp-connected-route-check
      bgp bestpath as-path multipath-relax
      neighbor TRANSIT peer-group
      neighbor TRANSIT remote-as external
      neighbor TRANSIT ebgp-multihop 1
      neighbor D1D3P1 interface peer-group TRANSIT
      neighbor D1D3P2 interface peer-group TRANSIT
      neighbor D1D4P1 interface peer-group TRANSIT
      neighbor D1D4P2 interface peer-group TRANSIT
      address-family ipv4 unicast
      redistribute connected
      neighbor TRANSIT activate
      exit-address-family
      address-family ipv6 unicast
      redistribute connected
      neighbor TRANSIT activate
      exit-address-family
      end
      exit
    deconfig: |
      no router bgp 65202
      end
      exit

spine1:
  sonic:
    config: |
      sudo counterpoll tunnel enable 
      sudo config interface startup D2D3P1 
      sudo config interface startup D2D3P2
      sudo config interface startup D2D4P1
      sudo config interface startup D2D4P2
      sudo config interface ipv6 enable use-link-local-only D2D3P1 
      sudo config interface ipv6 enable use-link-local-only D2D3P2
      sudo config interface ipv6 enable use-link-local-only D2D4P1 
      sudo config interface ipv6 enable use-link-local-only D2D4P2
    deconfig: |
      sudo config interface ipv6 disable use-link-local-only D2D3P1 
      sudo config interface ipv6 disable use-link-local-only D2D3P2
      sudo config interface ipv6 disable use-link-local-only D2D4P1 
      sudo config interface ipv6 disable use-link-local-only D2D4P2
      sudo counterpoll tunnel disable

  bgp:
    config: |
      no router bgp
      router bgp 65203
      no bgp ebgp-requires-policy
      no bgp default ipv4-unicast
      bgp disable-ebgp-connected-route-check
      bgp bestpath as-path multipath-relax
      neighbor TRANSIT peer-group
      neighbor TRANSIT remote-as external
      neighbor TRANSIT ebgp-multihop 1
      neighbor D2D3P1 interface peer-group TRANSIT
      neighbor D2D3P2 interface peer-group TRANSIT
      neighbor D2D4P1 interface peer-group TRANSIT
      neighbor D2D4P2 interface peer-group TRANSIT
      address-family ipv4 unicast
      redistribute connected
      neighbor TRANSIT activate
      exit-address-family
      address-family ipv6 unicast
      redistribute connected
      neighbor TRANSIT activate
      exit-address-family
      end
      exit
    deconfig: |
      no router bgp 65203
      end
      exit

leaf0:
  sonic:
    config: |
      sudo config interface startup D3D1P1
      sudo config interface startup D3D1P2
      sudo config interface startup D3D2P1
      sudo config interface startup D3D2P2
      sudo config interface startup D3T1P1
      sudo config interface ipv6 enable use-link-local-only D3D1P1
      sudo config interface ipv6 enable use-link-local-only D3D1P2
      sudo config interface ipv6 enable use-link-local-only D3D2P1
      sudo config interface ipv6 enable use-link-local-only D3D2P2
      sudo config interface ip add Loopback27 2001:db8:1::3/128
      sudo config vlan add 100
      sudo config interface ipv6 disable use-link-local-only D3T1P1
      sudo config vlan member add -u 100 D3T1P1
      sudo config vxlan add Vtep 2001:db8:1::3
      sudo config vxlan evpn_nvo add NVO Vtep
      sudo config vxlan map add Vtep 100 2727
      sudo counterpoll tunnel enable
      sudo counterpoll tunnel interval 2000
    deconfig: |
      sudo config vxlan map del Vtep 100 2727
      sudo config vxlan evpn_nvo del NVO
      sudo config vxlan del Vtep
      sudo config vlan member del 100 D3T1P1
      sudo config vlan del 100
      sudo config interface ip remove Loopback27 2001:db8:1::3/128
      sudo config interface ipv6 disable use-link-local-only D3D1P1
      sudo config interface ipv6 disable use-link-local-only D3D1P2
      sudo config interface ipv6 disable use-link-local-only D3D2P1
      sudo config interface ipv6 disable use-link-local-only D3D2P2

  bgp:
    config: |
      no router bgp
      router bgp 65200
      no bgp ebgp-requires-policy
      no bgp default ipv4-unicast
      bgp disable-ebgp-connected-route-check
      bgp bestpath as-path multipath-relax
      neighbor OVERLAY peer-group
      neighbor OVERLAY remote-as external
      neighbor OVERLAY disable-connected-check
      neighbor OVERLAY ebgp-multihop 255
      neighbor OVERLAY update-source Loopback27
      neighbor TRANSIT peer-group
      neighbor TRANSIT remote-as external
      neighbor TRANSIT ebgp-multihop 1
      neighbor 2001:db8:1::1 peer-group OVERLAY
      neighbor D3D1P1 interface peer-group TRANSIT
      neighbor D3D1P2 interface peer-group TRANSIT
      neighbor D3D2P1 interface peer-group TRANSIT
      neighbor D3D2P2 interface peer-group TRANSIT
      address-family ipv4 unicast
      redistribute connected
      neighbor TRANSIT activate
      exit-address-family
      address-family ipv6 unicast
      redistribute connected
      neighbor TRANSIT activate
      exit-address-family
      address-family l2vpn evpn
      neighbor OVERLAY activate
      advertise-all-vni
      advertise ipv4 unicast
      exit-address-family
      end
      exit
    deconfig: |
      no router bgp 65200
      end
      exit


leaf1:
  sonic:
    config: |
      sudo config interface startup D4D1P1
      sudo config interface startup D4D1P2
      sudo config interface startup D4D2P1
      sudo config interface startup D4D2P2
      sudo config interface startup D4T1P1
      sudo config interface ipv6 enable use-link-local-only D4D1P1
      sudo config interface ipv6 enable use-link-local-only D4D1P2
      sudo config interface ipv6 enable use-link-local-only D4D2P1
      sudo config interface ipv6 enable use-link-local-only D4D2P2
      sudo config interface ip add Loopback27 2001:db8:1::1/128
      sudo config vlan add 100
      sudo config interface ipv6 disable use-link-local-only D4T1P1
      sudo config vlan member add -u 100 D4T1P1
      sudo config vxlan add Vtep 2001:db8:1::1
      sudo config vxlan evpn_nvo add NVO Vtep
      sudo config vxlan map add Vtep 100 2727
      sudo counterpoll tunnel enable
      sudo counterpoll tunnel interval 2000
    deconfig: |
      sudo config vxlan map del Vtep 100 2727
      sudo config vxlan evpn_nvo del NVO
      sudo config vxlan del Vtep
      sudo config vlan member del 100 D4T1P1
      sudo config vlan del 100
      sudo config interface ip remove Loopback27 2001:db8:1::1/128
      sudo config interface ipv6 disable use-link-local-only D4D1P1
      sudo config interface ipv6 disable use-link-local-only D4D1P2
      sudo config interface ipv6 disable use-link-local-only D4D2P1
      sudo config interface ipv6 disable use-link-local-only D4D2P2

  bgp:
    config: |
      no router bgp
      router bgp 65201
      no bgp ebgp-requires-policy
      no bgp default ipv4-unicast
      bgp disable-ebgp-connected-route-check
      bgp bestpath as-path multipath-relax
      neighbor OVERLAY peer-group
      neighbor OVERLAY remote-as external
      neighbor OVERLAY disable-connected-check
      neighbor OVERLAY ebgp-multihop 255
      neighbor OVERLAY update-source Loopback27
      neighbor TRANSIT peer-group
      neighbor TRANSIT remote-as external
      neighbor TRANSIT ebgp-multihop 1
      neighbor 2001:db8:1::3 peer-group OVERLAY
      neighbor D4D1P1 interface peer-group TRANSIT
      neighbor D4D1P2 interface peer-group TRANSIT
      neighbor D4D2P1 interface peer-group TRANSIT
      neighbor D4D2P2 interface peer-group TRANSIT
      address-family ipv4 unicast
      redistribute connected
      neighbor TRANSIT activate
      exit-address-family
      address-family ipv6 unicast
      redistribute connected
      neighbor TRANSIT activate
      exit-address-family
      address-family l2vpn evpn
      neighbor OVERLAY activate
      advertise-all-vni
      advertise ipv4 unicast
      exit-address-family
      end
      exit
    deconfig: |
      no router bgp 65201
      end
      exit

