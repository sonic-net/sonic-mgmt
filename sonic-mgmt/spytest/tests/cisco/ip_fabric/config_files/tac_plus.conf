key = testsonic

user = test {
    default service = permit
    pap = cleartext "test"
    service = exec {
                priv-lvl = 15
        }
}

user = test1 {
    default service = permit
    pap = cleartext "test"
    service = exec {
                priv-lvl = 1
        }
}
