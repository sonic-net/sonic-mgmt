ansible_connection: multi_passwd_ssh

ansible_user: root
ansible_password: root
# ansible_altpasswords: "{{ secret_group_vars['str']['altpasswords'] }}"
ansible_altpassword: fakepassword1
ansible_altpasswords:
  - fakepassword2
  - fakepassword3
  - fakepassword
