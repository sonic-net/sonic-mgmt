[Unit]
Description=SNMP container
Requires=database.service
After=database.service

[Service]
User={{ sonicadmin_user }}
ExecStart=/usr/bin/docker start -a snmp
ExecStop=/usr/bin/docker stop snmp

[Install]
WantedBy=multi-user.target
