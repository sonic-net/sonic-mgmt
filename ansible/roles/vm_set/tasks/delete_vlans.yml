# FIXME: my ansible doesn't support nested loops. So below are copy of everything
- name: Remove external interface. Vlan 0. vm set {{ id }}
  include: delete_vlan.yml
  vars:
    port0_bridge: "br-vs{{ id }}-vm{{ num }}-0"

- name: Remove external interface. Vlan 1. vm set {{ id }}
  include: delete_vlan.yml
  vars:
    port0_bridge: "br-vs{{ id }}-vm{{ num }}-1"

- name: Remove external interface. Vlan 2. vm set {{ id }}
  include: delete_vlan.yml
  vars:
    port0_bridge: "br-vs{{ id }}-vm{{ num }}-2"

- name: Remove external interface. Vlan 3. vm set {{ id }}
  include: delete_vlan.yml
  vars:
    port0_bridge: "br-vs{{ id }}-vm{{ num }}-3"

- name: Remove external interface. Vlan 4. vm set {{ id }}
  include: delete_vlan.yml
  vars:
    port0_bridge: "br-vs{{ id }}-vm{{ num }}-4"

- name: Remove external interface. Vlan 5. vm set {{ id }}
  include: delete_vlan.yml
  vars:
    port0_bridge: "br-vs{{ id }}-vm{{ num }}-5"

- name: Remove external interface. Vlan 6. vm set {{ id }}
  include: delete_vlan.yml
  vars:
    port0_bridge: "br-vs{{ id }}-vm{{ num }}-6"

- name: Remove external interface. Vlan 7. vm set {{ id }}
  include: delete_vlan.yml
  vars:
    port0_bridge: "br-vs{{ id }}-vm{{ num }}-7"

