{%- if features | length > 0 %}
{ "op": "add", "path": "/FEATURE", "value": {} },
{%- for feature in features %}
{ "op": "add", "path": "/FEATURE/{{ feature.name }}", "value": { "auto_restart": "{{ feature.auto_restart }}", "state": "{{ feature.state }}" } }{{ "," if not loop.last else "" }}
{% endfor %}
,
{%- endif %}
