[Unit]
Description=TEAMD container
Requires=database.service
After=database.service

[Service]
User={{ sonicadmin_user }}
ExecStart=/usr/bin/docker start -a teamd
ExecStop=/usr/bin/docker stop teamd

[Install]
WantedBy=multi-user.target
