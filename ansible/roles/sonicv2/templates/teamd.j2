{
    "device": "{{port_channel["name"]}}",
    "runner": {
        "name": "lacp",
        "active": true,
        "fast_rate": true,
        "tx_hash": ["eth", "ipv4", "ipv6"],
    },
    "link_watch": {
        "name": "ethtool",
    },
    "ports": {
        {% for member in port_channel["members"] %}
        "{{member}}": {},
        {% endfor %}
    }
}

