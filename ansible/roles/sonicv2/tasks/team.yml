- name: Start the team docker container
  include: sonicdocker.yml
  vars:
    docker_container: team
    docker_image: "{{ image_id_team }}"
    docker_privileged: yes
    docker_state: reloaded

- include: team_interface.yml
  with_items: "{{ minigraph_portchannel_interfaces }}"
