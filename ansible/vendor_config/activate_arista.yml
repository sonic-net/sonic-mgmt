- name: activate arista config
  activate_arista: telnet_port={{ serial_port }}
                   login={{ eos_default_login }}
                   password={{ sonic_password }}
  register: activate_output
  until: '"activate_code" in activate_output and activate_output.activate_code == 0'
  retries: 5
  delay: 10
