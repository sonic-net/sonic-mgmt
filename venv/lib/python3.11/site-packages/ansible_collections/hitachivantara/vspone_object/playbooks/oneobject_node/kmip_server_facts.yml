---
- name: Get KMIP servers module
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../ansible_vault_vars/oneobject_node_connection_variables.yml
  vars:
    connection_info:
      http_request_timeout: "{{ http_request_timeout }}"
      http_request_retry_times: "{{ http_request_retry_times }}"
      http_request_retry_interval_seconds: "{{ http_request_retry_interval_seconds }}"
      ssl: "{{ ssl }}"
      cluster_name: "{{ cluster_name }}"
      region: "{{ region }}"
      oneobject_node_username: "{{ oneobject_node_username }}"
      oneobject_node_userpass: "{{ oneobject_node_userpass }}"
      oneobject_node_client_id: "{{ oneobject_node_client_id }}"

  tasks:
    ####################################################################
    # Task 1 : List KMIP servers
    ####################################################################
    - name: List KMIP servers
      hitachivantara.vspone_object.oneobject_node.hv_kmip_server_facts:
        connection_info: "{{ connection_info }}"
      register: result

    - name: Display result
      ansible.builtin.debug:
        var: result
    ####################################################################
    # Task 2 : Get KMIP server by name
    ####################################################################
    - name: Get KMIP server by name
      hitachivantara.vspone_object.oneobject_node.hv_kmip_server_facts:
        connection_info: "{{ connection_info }}"
        spec:
          name: "sample_kmip_server"
      register: result

    - name: Display result
      ansible.builtin.debug:
        var: result
